**TestCase1**

1.Navigate to the facebook application.
2.Click on create new account button.
3.Verify that fistname and surname textfields are aligned in same line or not.
---------------------------------------------------------------------------------------

package selenium_Assessment;

import java.time.Duration;

import org.openqa.selenium.By;
import org.openqa.selenium.Point;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;

public class Facebook {
	public static void main(String[] args) {
		WebDriver driver = new ChromeDriver();
		driver.manage().window().maximize();
		driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(10));
		driver.get("https://www.facebook.com/");
		driver.findElement(By.xpath("//span[text()='Create new account']")).click();
		WebElement firstname = driver.findElement(By.id("_R_1cl2p4jikacppb6amH1_"));
		WebElement surname = driver.findElement(By.id("_R_1kl2p4jikacppb6amH1_"));
		Point firstName = firstname.getLocation();
		Point surName = surname.getLocation();
		System.out.println("First location:"+firstName);
		System.out.println("Surname Location:"+surName);
		if(firstName.getY() == surName.getY()) {
			System.out.println("FirstName and surname are alligned in single line");
		}
		else {
			System.out.println("Not alligned");
		}
		
	}

}
---------------------------------------------------------------------------------------------------
**TestCase2**

1.Navigate to facebook application 
2.capture the location of create new account button.
3.Capture the Dom Attribute and dom property for email address textfield before and after entering email address
4.click on create new account capture the size of submit button
5.capture the css properties of submit button (minimum 3).
------------------------------------------------------------------------------

package selenium_Assessment;

import java.time.Duration;

import org.openqa.selenium.By;
import org.openqa.selenium.Point;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;

public class FacebookTC2 {
	public static void main(String[] args) throws InterruptedException {
		WebDriver driver = new ChromeDriver();
		driver.manage().window().maximize();
		driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(10));
		driver.get("https://www.facebook.com/");
		WebElement loc = driver.findElement(By.xpath("//span[text()='Create new account']"));
		Point Location = loc.getLocation();
		System.out.println(Location);
		loc.click();
		
		WebElement email = driver.findElement(By.id("_R_6ad8p4jikacppb6amH1_"));;
		
		System.out.println(email.getDomAttribute("value"));

		System.out.println(email.getDomProperty("value"));
		
		email.sendKeys("admin@gamil.com");
		
		Thread.sleep(2000);
		
		System.out.println(email.getDomAttribute("value"));

		System.out.println(email.getDomProperty("value"));
		
		
		WebElement button = driver.findElement(By.xpath("//span[normalize-space()='Submit']/ancestor::div[@role='none'][2]"));
		
		System.out.println(button.getSize());
		System.out.println(button.getSize().getHeight());
		System.out.println(button.getSize().getWidth());
		
		
		System.out.println(button.getCssValue("line-height"));

		System.out.println(button.getCssValue("font-size"));
		
		System.out.println(button.getCssValue("font-weight"));
		
		System.out.println(button.getCssValue("background-color"));
		
	}

}
--------------------------------------------------------------------------------------------------------------------------------
**TestCase3**
1.Navigate to demowebshop
2.Identify the webelement(14.1-inch laptop)-->identify add to cart button capture the size and them click on the buuton.
3.Click on shopping cart link.
4.verify remove check box is selected or not and capture the complete information using getRect().
5.Verify Alert message is displayed or not before and after clicking on Apply coupon button
6.Take screenshot of image(Laptop).
----------------------------------------------------------------

package selenium_Assessment;

import java.io.File;
import java.io.IOException;
import java.time.Duration;

import org.openqa.selenium.By;
import org.openqa.selenium.OutputType;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.io.FileHandler;

public class DemoWebShopTC3 {

	public static void main(String[] args) throws InterruptedException, IOException {

		WebDriver driver = new ChromeDriver();

		driver.manage().window().maximize();

		driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(10));

		// Navigate to DemoWebShop
		driver.get("https://demowebshop.tricentis.com/");

		
		driver.findElement(By.xpath("//a[@title='Show details for 14.1-inch Laptop']")).click();

		// Add to cart
		WebElement cart = driver.findElement(By.id("add-to-cart-button-31"));

		System.out.println("Add to Cart Size : " + cart.getSize());

		cart.click();

		Thread.sleep(2000);

		// Go to Shopping Cart
		driver.findElement(By.xpath("//a[@class='ico-cart']")).click();

		Thread.sleep(2000);

		WebElement removeCheckbox = driver.findElement(
				By.name("removefromcart"));

		if (removeCheckbox.isSelected()) {

			System.out.println("Remove checkbox is selected");

		} else {

			System.out.println("Remove checkbox is not selected");
		}

		// Capture complete information using getRect()
		System.out.println("X : "+ removeCheckbox.getRect().getX());

		System.out.println("Y : "+ removeCheckbox.getRect().getY());

		System.out.println("Width : "+ removeCheckbox.getRect().getWidth());

		System.out.println("Height : "+ removeCheckbox.getRect().getHeight());

		System.out.println("Complete Rect : "+ removeCheckbox.getRect());

		if (driver.findElements(By.className("message")).size() > 0) {

			System.out.println("Alert message is displayed BEFORE clicking Apply coupon");

		} else {

			System.out.println("Alert message is NOT displayed BEFORE clicking Apply coupon");
		}


		// Apply coupon
		WebElement applyCoupon = driver.findElement(By.xpath("//input[@value='Apply coupon']"));

		applyCoupon.click();

		Thread.sleep(2000);

		if (driver.findElements(By.className("message")).size() > 0) {

			WebElement message = driver.findElement(By.className("message"));

			if (message.isDisplayed()) {

				System.out.println(
						"Alert message is displayed AFTER clicking Apply coupon");

				System.out.println("Message : " + message.getText());
			}

		} else {

			System.out.println(
					"Alert message is NOT displayed AFTER clicking Apply coupon");
		}
		Thread.sleep(2000);

		WebElement laptopImage = driver.findElement(By.xpath("//a[contains(text(),'14.1-inch Laptop')]/ancestor::tr//img"));
		File src = laptopImage.getScreenshotAs(OutputType.FILE);
		File dest = new File("./Ss/Laptop.png");
		FileHandler.copy(src, dest);

		System.out.println("Laptop screenshot captured successfully.");

		
		driver.quit();
	}
}
